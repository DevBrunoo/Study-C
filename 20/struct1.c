typedef struct node /* Comecamos a strutura com typedef struct node para indicar que que estamos nos referindo com uma struct node dentro de nossa estrutura */
{
    int number;
    struct node *next;
}
node;

/* 

Chamaremos o grupo de caixas com um valor e ponteiro de nó , um componente de uma estrutura de dados encapsula algumas informações. Podemos implementar um nó com uma estrutura:


 */