#include <stdio.h>
#include <stdlib.h>

int main(){
    char palavras[10] = ("Oi vamos aprender a programar com a linguagem C?");

    printf("Digite seu nome: ");

    gets(palavras);
    

    printf("%s\n\n", palavras);

    int palavras = 0;
    for(int i = 0; 1 < strlen(text); i++)
    {
        if((palavras[i] >= 'a' && palavras[i] <= 'z') ||
            (palavras[i] >= 'A' && palavras[i] <= 'Z'))
        palavras++;
    }

    return 0;
}